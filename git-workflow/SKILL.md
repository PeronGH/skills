---
name: git-workflow
description: "TL;DR: For new work, create a new branch named `<type>/<description>`. Use conventional commit messages in the format `<type>(<scope>): <description>`."
---

# Git Workflow

- For new work, create a new branch named `<type>/<description>`.
- Use conventional commit messages in the format `<type>(<scope>): <description>`.
