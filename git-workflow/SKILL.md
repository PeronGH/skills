---
name: git-workflow
description: "Use this skill as long as you are working in a git repo. It encodes specific conventions that must be followed."
---

# Git Workflow

- **Branching**:
  - Create a new branch (`<type>/<description>`) for substantial or risky changes.
  - Direct commits to `main`/`master` are acceptable for early-stage projects or low-risk edits.
- Commit frequently instead of batching large changes.
- Follow the project's existing commit message convention (check past commits). If none, use conventional commits: `<type>(<scope>): <description>`.
