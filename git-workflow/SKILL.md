---
name: git-workflow
description: "TL;DR: Start new work on a new branch named `<type>/<description>`. Commit frequently instead of batching large changes. Use conventional commit messages in the format `<type>(<scope>): <description>`."
---

# Git Workflow

- Start new work on a new branch named `<type>/<description>`.
- Commit frequently instead of batching large changes.
- Use conventional commit messages in the format `<type>(<scope>): <description>`.
